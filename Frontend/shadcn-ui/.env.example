VITE_API_BASE_URL=http://127.0.0.1:8776
# Opcional: si el backend exige autenticación para POST /reservations
# VITE_API_KEY=tu_api_key
# Durante desarrollo, usa datos mock cuando el backend no esté disponible (auto|force|off)
# VITE_USE_MOCKS=auto
# Mostrar páginas de Clientes y Estadísticas (solo para demos a clientes, desactivar en producción)
# VITE_ENABLE_FUTURE_FEATURES=true
